language: node_js
node_js:
  - 12.9.1
cache: npm

install: 
  - npm ci

script:
  - yarn test
  - yarn run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master